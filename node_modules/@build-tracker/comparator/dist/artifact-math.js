"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.delta = function (key, baseSizes, prevSizes) {
    if (!baseSizes) {
        if (!prevSizes) {
            return 0;
        }
        return -prevSizes[key];
    }
    else if (!prevSizes) {
        return baseSizes[key];
    }
    return baseSizes[key] - prevSizes[key];
};
exports.percentDelta = function (key, baseSizes, prevSizes) {
    if (!baseSizes) {
        if (!prevSizes) {
            return 0;
        }
        return -1;
    }
    if (!prevSizes) {
        return 1;
    }
    var base = prevSizes[key];
    var changed = baseSizes[key];
    if (base === 0) {
        return changed === 0 ? 0 : 1;
    }
    else if (changed > base) {
        var delta_1 = changed - base;
        return delta_1 / base;
    }
    else if (changed < base) {
        var delta_2 = base - changed;
        return -(delta_2 / base);
    }
    return 0;
};
//# sourceMappingURL=artifact-math.js.map